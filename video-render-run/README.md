# Video Render Run

Companion entry point for the [`video-builder`](../video-builder/) family. It
uses the renderer in `video-builder/scripts/`, so install both directories.

License: MIT; see [`video-builder/LICENSE`](../video-builder/LICENSE).
