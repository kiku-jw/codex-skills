interface:
  display_name: "Spec Bundle"
  short_description: "Build an implementation-ready bundle with contracts and architecture gates"
  default_prompt: "Turn this PRD or raw spec into an implementation-ready bundle with only the artifacts that reduce ambiguity, including blueprint, gates, and ADRs when warranted."
